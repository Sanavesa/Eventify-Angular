import { KeywordValidatorDirective } from './keyword-validator.directive';

describe('KeywordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
